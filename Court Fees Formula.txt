=IF(A2<=100, ROUNDUP(A2/5, 0)*0.6, IF(A2<=1000, ROUNDUP((A2-100)/10, 0)*1.1+12, IF(A2<=10000, ROUNDUP((A2-1000)/100, 0)*7.5+111, IF(A2<=20000, ROUNDUP((A2-10000)/500, 0)*30+786, IF(A2<=30000, ROUNDUP((A2-20000)/1000, 0)*40+1386, IF(A2<=50000, ROUNDUP((A2-30000)/2000, 0)*60+1786, IF(A2<=100000, ROUNDUP((A2-50000)/4000, 0)*80+2386, ROUNDUP((A2-100000)/10000, 0)*100+3426))))))). This is the formula. here A2 is the cell where user inputs value. 


I want you to write xml code. there will be only one screen present which will have blocks. first block is used for entering user value which would have title "enter value".this black should be big enough. the second block would display the output of the calculation. it should be black by default and have a background colour lighter than block colour so as to differentiate. use dark themed colours exactly the colours which are used in iPhone default calculator app. and also display numeric keyboard at bottom of app like a calculator with back button and reset button. in that numeric keypad there should be no maths calculation symbols like addition, subtraction or multiplication or division